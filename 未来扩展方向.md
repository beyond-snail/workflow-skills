# 未来扩展方向

## 说明

下面这些方向不是当前 skill 明写的正式规划，而是根据现有结构推演出来的自然扩展路径。

## 1. 从三段扩成多阶段

当前是：

`bootstrap -> requirement -> execution`

后续可以自然补出更多阶段，例如：

- `review`：专门做需求或设计评审
- `qa`：专门做测试与验收回写
- `release`：专门做发布前门禁
- `archive`：专门做任务归档和知识沉淀

这意味着 workflow 未来可能从“三 skill”扩成“一套阶段化工作流”。

在这个基础上，老项目画像能力已经让 `bootstrap` 从“只建底座”走向“先理解项目，再建底座”，后续阶段扩展也应该默认继承这种“先扫描上下文”的思路。

## 2. 从脚本编排升级成规则引擎

现在很多逻辑是写在脚本里的，比如：

- 先选 `doing`，再选最早 `todo`
- 根据 summary 判断是否建议补 `issue` / `decision`
- 根据任务状态决定是否归档

未来可以把这些逻辑抽象成可配置规则，例如：

- 不同项目有不同任务选择策略
- 不同任务类型有不同闸门
- 不同团队有不同 memory 回写策略

## 3. 从通用模板变成项目适配层

现有设计已经有 profile 和仓库探测，下一步可以更强调项目级适配：

- 不同语言的默认命令不同
- 不同仓库的需求池字段不同
- 不同团队的交接门槛不同
- 不同老项目的业务域、接口链路、模块边界识别策略不同

这会让 workflow 从“通用工作流”继续向“项目化工作流”演进。

## 4. 从文档驱动升级到证据驱动

目前已经有证据、verify、test result、acceptance 这些文件，后续可以进一步做：

- 自动聚合每次执行的证据链
- 自动生成回顾报告
- 自动识别重复问题和高频决策
- 自动把稳定结论提升到 knowledge
- 自动把 legacy-scan、requirement、execution 的关键信息合并进统一项目画像

这样 workflow 就不只是执行记录，而是逐步形成项目知识系统。

## 5. 从单仓库能力扩到跨仓库协作

现在已经考虑 Codex / Claude 宿主一致性了，未来可以继续扩展到：

- 多仓库统一治理
- 子仓库引用主仓库 requirement
- 多 agent 协作共享 memory 和 knowledge
- 跨项目复用任务模板
- 跨项目复用业务域画像和接口链路识别规则

这会让它从单仓库工具变成组织级协作协议。

## 6. 从命令式使用变成更强的交互层

现在主要靠命令和 markdown 文件，未来还可以加：

- workflow 状态面板
- 任务记忆浏览器
- 需求和任务关系图
- 证据链可视化
- 老项目画像可视化
- 接口链路命中与复用可视化

这样非技术角色也更容易查看和审核。

## 7. 最值得优先的三个方向

如果只选三个，我会优先：

1. 增加更明确的阶段，比如 `review` / `qa` / `release`
2. 把规则从脚本里抽出来，做成可配置策略
3. 强化证据、legacy context 和知识沉淀，让 workflow 变成项目记忆系统

## 一句话总结

workflow 的未来扩展，不是单纯加功能，而是从“有流程”走向“有规则、有证据、有知识、有协作层次”。
