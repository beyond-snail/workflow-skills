# workflow 三 Skill 分享话术

## 使用方式

这份文档是给分享时直接照着讲的，不是练习题。

它和下面这些材料配套使用：

- [NotebookLM资料索引.md](./NotebookLM资料索引.md)
- [流程总览说明.md](./流程总览说明.md)
- [三个skill分析.md](./三个skill分析.md)
- [使用方法与误区.md](./使用方法与误区.md)
- [操作步骤说明.md](./操作步骤说明.md)
- [PPT页纲.md](./PPT页纲.md)

## 开场

“今天我想分享的是三套 skill 的设计逻辑、边界和实际用法，不是讲练习，也不是讲怎么照着抄命令。你可以把它理解成一条仓库内的治理链路：先把底座搭起来，再把需求治理好，最后在审核通过后进入执行。”

“这三套 skill 不是平级工具，而是按顺序工作的。`workflow-bootstrap` 负责建底座，`workflow-requirement` 负责把 PRD 变成需求包和任务看板，`workflow-execution` 负责在显式开工后把任务真正收口。”

## 第 1 页：为什么会有 workflow 三 skill

“先看为什么要拆成三段。原因很简单：初始化、需求治理、执行收口，是三个完全不同的问题。如果把它们混在一起，仓库事实、需求边界、证据链和任务状态都会混乱。”

“所以这套 workflow 的目标，不是让 AI 更忙，而是让 AI 在正确阶段做正确的事。”

## 第 2 页：三阶段总览

“这三套 skill 的顺序是固定的：先 `bootstrap`，再 `requirement`，最后 `execution`。它们不是可互换的模块，而是一个闭环的三个门。”

“`bootstrap` 先把仓库变成可治理状态，`requirement` 把 PRD 沉淀成可交接材料，`execution` 在审核通过后完成实现和收口。”

## 第 3 页：workflow-bootstrap 解决什么问题

“先看 `workflow-bootstrap`。它解决的是一个最基础的问题：这个仓库有没有统一事实、统一目录和统一协作规则。如果没有这一步，后面 requirement 和 execution 都会漂。”

“它现在不只是新项目初始化工具。对老项目来说，`wf-init` 会自动扫描仓库现状，识别旧底座、旧记忆目录、业务域、关键接口链路，再生成新的 workflow 底座。”

“它会自动识别语言、构建工具、测试命令、PRD 目录和源码目录，然后生成 `AGENTS.md`、`docs/workflow/PROJECT_CONTEXT.md`、`.ai/`、profile，以及统一状态骨架 `project-state.json`。它做的是仓库治理底座，不是业务实现。”

## 第 4 页：workflow-bootstrap 的输出

“bootstrap 的输出不应该被理解成业务产物，而是协作产物。它交付的是协作规则、项目上下文、任务记忆骨架、profile 配置，还有 `docs/workflow/requirements/` 的治理骨架。”

“如果是老项目，它还会额外交付两样很关键的东西：一个是 `docs/workflow/legacy-analysis.md`，一个是 `.ai/runtime/cache/legacy-scan.json`。前者给人看，后者给后续 skill 复用。”

“换句话说，这一步是在给后面两套 skill 造地基。”

## 第 5 页：workflow-requirement 解决什么问题

“有了底座以后，下一步才是 `workflow-requirement`。它做的不是开发，而是把正式 PRD 沉淀成需求池、任务看板和交接材料。”

“这里最关键的是，它现在不只是拆任务。它还会自动复用 bootstrap 阶段扫出来的老项目画像，把命中的业务域、接口链路和历史文档上下文带进需求包里。”

“它会拆任务，但默认停在人工审核门，不会直接进入开发。这个门是故意设的，因为如果需求还没收口就进入执行，后面很容易出现边界不清、状态漂移和证据链断裂。”

## 第 6 页：workflow-requirement 的边界

“requirement 的边界要讲清楚：它会拆任务、补材料，但不会写业务代码，也不会跑构建和测试。它的目标是把 PRD 变成可交接、可审计、可追溯的需求治理材料，而不是马上开始实现。”

“所以这一步的结果不是‘可以开干’，而是‘已经准备好交给人审核’。”

## 第 7 页：workflow-execution 解决什么问题

“等 requirement 通过人工审核后，才轮到 `workflow-execution`。这一步必须有显式开工指令，不然不能启动。”

“它不是只帮你改几处代码，而是把一次开发回合完整收口：先选任务，再读取 memory、knowledge 和 legacy context，然后跑构建和测试，写证据、写 verify、回写 issue 或 decision，最后提交、推送、跑 release gate，必要时归档 task memory。”

## 第 8 页：workflow-execution 的边界

“execution 的核心是收口，不是单纯的代码修改器。它默认会做很多事情，所以边界要更严格：没有显式开工不能启动，不能只改代码不留痕，任务状态和证据必须同步。”

“真正完成不是代码改完，而是实现、验证、证据、状态、提交和闸门都同步完成。”

## 第 9 页：三者如何串联

“如果把这三步连起来看，逻辑其实很清楚：bootstrap 提供仓库事实和老项目画像，requirement 提供需求事实，execution 提供执行事实。三者共同维护的是同一套事实源、同一套证据链和同一套任务状态。”

“这也是它为什么适合多人、多轮、跨窗口接力。因为每一步都有落点，不会只停留在口头说明。”

## 第 10 页：常见误用

“最容易出问题的地方也很固定。第一，没有 bootstrap 就直接 requirement，后面事实基础不稳。第二，requirement 还没审核就进 execution，等于跳过治理门。第三，execution 没有显式开工就执行，阶段边界被破坏。第四，只改代码，不回写证据和 memory，闭环就不完整。”

“这些误用本质上都不是命令问题，而是阶段意识的问题。”

## 第 11 页：最后结论

“这套 workflow 的价值，不在于让 AI 自动干活，而在于让 AI 在正确的阶段做正确的事，并且每一步都留下正确的证据。”

“真正重要的不是多做了什么，而是知道什么时候该停。只要这个边界清楚，这三套 skill 就能形成一条稳定的协作链路。”

## 收尾

“如果把今天的分享压缩成一句话，就是：先建底座，再做需求治理，最后显式开工完成收口。三套 skill 的意义，不是把流程写得更长，而是把协作做得更稳。”
