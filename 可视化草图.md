# workflow 三 Skill 可视化草图

## 使用说明

这份文档用于把前面的分析材料转成可视化结构，便于后续做 PPT、讲稿和 NotebookLM 汇总。

---

## 1. 总流程图

```mermaid
flowchart LR
    A[workflow-bootstrap<br/>仓库底座初始化] --> B[workflow-requirement<br/>需求治理]
    B --> C[workflow-execution<br/>执行收口]

    A --> A1[输入：仓库根目录<br/>profile 线索<br/>宿主环境]
    A --> A2[输出：AGENTS<br/>PROJECT_CONTEXT<br/>.ai/ .ai/runtime/profile]

    B --> B1[输入：PRD<br/>主题<br/>仓库事实]
    B --> B2[输出：需求池<br/>任务看板<br/>任务记忆<br/>交接结论]

    C --> C1[输入：审核通过<br/>显式开工<br/>任务看板<br/>memory/knowledge]
    C --> C2[输出：代码<br/>验证<br/>证据<br/>提交/闸门]

    style A fill:#E8F4FF,stroke:#3B82F6,stroke-width:2px
    style B fill:#FFF7E6,stroke:#F59E0B,stroke-width:2px
    style C fill:#EAF7EF,stroke:#10B981,stroke-width:2px
```

### 讲解要点

- 三段式不是并列工具，而是固定顺序的闭环
- 每一步都有自己的输入、输出和停止点
- `workflow-requirement` 和 `workflow-execution` 之间有人工审核门

---

## 2. 三 Skill 对照图

```mermaid
flowchart TB
    subgraph S1[workflow-bootstrap / 仓库底座初始化]
        A1[解决问题：仓库有没有统一事实和协作规则]
        A2[主入口：init_workflow_bootstrap.py]
        A3[关键产物：AGENTS / PROJECT_CONTEXT / .ai / profile]
        A4[边界：不做需求整理，不做执行]
        A1 --> A2 --> A3 --> A4
    end

    subgraph S2[workflow-requirement / 需求治理]
        B1[解决问题：PRD 有没有被治理成可交接材料]
        B2[主入口：run_requirement_round.py]
        B3[关键产物：需求池 / 任务看板 / task memory / 交接结论]
        B4[边界：停在人工审核门，不进入开发]
        B1 --> B2 --> B3 --> B4
    end

    subgraph S3[workflow-execution / 执行收口]
        C1[解决问题：任务是否完成实现、验证和收口]
        C2[主入口：run_execution_round.py]
        C3[关键产物：代码 / 验证 / 证据 / 提交 / 闸门]
        C4[边界：必须显式开工，不能只改代码不留痕]
        C1 --> C2 --> C3 --> C4
    end

    S1 --> S2 --> S3
```

### 讲解要点

- 三个 skill 分别解决“底座问题”“需求问题”“执行问题”
- 不要把它们理解成同一层的脚本
- 每个 skill 的输出，正好是下一个 skill 的输入基础

---

## 3. 误用对照图

```mermaid
flowchart LR
    M1[误用 1：跳过 bootstrap] --> R1[后果：仓库事实不稳<br/>profile 不准<br/>后续治理漂移]
    M2[误用 2：requirement 直接进 execution] --> R2[后果：跳过人工审核门<br/>边界失控<br/>证据链断裂]
    M3[误用 3：execution 没有显式开工] --> R3[后果：阶段边界被破坏<br/>误执行<br/>状态混乱]
    M4[误用 4：只改代码不回写证据] --> R4[后果：闭环不完整<br/>复盘困难<br/>接力失真]

    F1[正确路径：先 bootstrap] --> F2[再 requirement]
    F2 --> F3[审核通过后 execution]
    F3 --> F4[同步证据 / memory / 状态]

    style M1 fill:#FEE2E2,stroke:#EF4444
    style M2 fill:#FEE2E2,stroke:#EF4444
    style M3 fill:#FEE2E2,stroke:#EF4444
    style M4 fill:#FEE2E2,stroke:#EF4444
    style F1 fill:#DCFCE7,stroke:#22C55E
    style F2 fill:#DCFCE7,stroke:#22C55E
    style F3 fill:#DCFCE7,stroke:#22C55E
    style F4 fill:#DCFCE7,stroke:#22C55E
```

### 讲解要点

- 误用的本质不是命令错，而是阶段边界错
- 最容易被忽略的是审核门和证据链
- 正确路径的重点不是“做更多”，而是“按顺序做对”

