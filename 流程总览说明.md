# 流程总览说明

## 一句话定义

这条链路把 AI 协作开发拆成三段：

1. `bootstrap` 把仓库变成可治理状态
2. `requirement` 把 PRD 变成可交接材料
3. `execution` 在审核后把任务真正收口

补充一点：现在这条链路已经同时覆盖新项目和老项目。

- 新项目：直接初始化底座
- 老项目：`wf-init` 会自动扫描仓库现状、识别旧底座资产、生成业务画像与迁移建议，再补齐新底座

## 建议中文名称

为了分享时更好讲，可以给三个 skill 配中文名称：

1. `workflow-bootstrap` = 仓库底座初始化
2. `workflow-requirement` = 需求治理
3. `workflow-execution` = 执行收口

## 总流程

```text
bootstrap -> requirement -> execution
```

## 为什么要分三段

如果把初始化、需求治理、执行收口混在一起，常见问题会同时出现：

- 没有统一项目上下文
- PRD 没有先冻结
- 任务拆解和实现边界混乱
- 验证、证据、任务状态不同步
- memory 无法沉淀

三段式的价值就是把这些问题拆开治理。

## 流程门槛

### bootstrap 门槛

- 目标是把仓库基础设施补齐
- 输出的是统一规则、上下文、profile、memory 骨架，以及运行态事实源
- 没有这一步，后续两步没有稳定底座
- 现在默认自动识别新项目 / 老项目，不要求人工先声明

### requirement 门槛

- 目标是把正式 PRD 沉淀成需求包
- 输出的是需求池、任务看板、设计目录、PRD 追溯、task memory，以及命中的老项目画像上下文
- 必须停在人工审核门

### execution 门槛

- 目标是把审核后的任务推进到实现收口
- 输出的是代码、验证、证据、状态、提交、闸门、归档，以及回写后的统一项目状态
- 必须有显式开工指令

## 三段的输入输出

| 阶段 | 输入 | 输出 | 停止点 |
| --- | --- | --- | --- |
| bootstrap | 仓库根目录、宿主环境、现有文档/代码/旧底座线索 | `AGENTS.md`、`docs/workflow/PROJECT_CONTEXT.md`、`.ai/`、`docs/workflow/requirements/`、`.ai/runtime/project-state.json`、`docs/workflow/legacy-analysis.md`、`.ai/runtime/cache/legacy-scan.json` | 初始化完成 |
| requirement | PRD、主题、仓库事实、profile、legacy-scan 缓存 | 需求池、任务看板、dated 需求包、task memory、handoff 结论、命中的 legacy context、更新后的 `project-state.json` | 人工审核门 |
| execution | 审核通过、显式开工、任务看板、memory、profile、legacy-scan 缓存 | 实现、验证、证据、任务状态、commit、push、release gate、更新后的 `project-state.json` | done / blocked |

## 关键目录

- `AGENTS.md`
- `docs/workflow/PROJECT_CONTEXT.md`
- `docs/workflow/开发协作约定.md`
- `docs/workflow/requirements/`
- `.ai/governance/`
- `.ai/memory/`
- `.ai/runtime/profile/`
- `.ai/runtime/project-state.json`
- `.ai/runtime/cache/legacy-scan.json`

## 关键文件

- `.ai/runtime/profile/project-profile.yml`
- `.ai/runtime/project-state.json`
- `.ai/memory/tasks/index.md`
- `docs/workflow/requirements/需求池.md`
- `docs/workflow/requirements/任务看板.md`
- `docs/workflow/legacy-analysis.md`

## 统一入口

这三套 skill 在命令层不是彼此孤立的，统一入口由 `workflow-bootstrap` 目录下的 CLI 总控脚本串起来。

- `init / bootstrap`
- `health / doctor`
- `requirement / req`
- `execution / exec`
- `archive / arc`

## 这条链路的价值

- 让 AI 协作有统一事实源
- 让老项目接入时自动形成业务画像和接口链路初稿
- 让需求和实现有清晰边界
- 让验证、证据、记忆形成闭环
- 让项目在多人和多轮 AI 接力下仍然可控
