# 流程总览说明

## 一句话定义

这条链路把 AI 协作开发拆成三段：

1. `bootstrap` 把仓库变成可治理状态
2. `requirement` 把 PRD 变成可交接材料
3. `execution` 在审核后把任务真正收口

## 建议中文名称

为了分享时更好讲，可以给三个 skill 配中文名称：

1. `workflow-bootstrap` = 仓库底座初始化
2. `workflow-requirement` = 需求治理
3. `workflow-execution` = 执行收口

## 总流程

```text
bootstrap -> requirement -> execution
```

## 为什么要分三段

如果把初始化、需求治理、执行收口混在一起，常见问题会同时出现：

- 没有统一项目上下文
- PRD 没有先冻结
- 任务拆解和实现边界混乱
- 验证、证据、任务状态不同步
- memory 无法沉淀

三段式的价值就是把这些问题拆开治理。

## 流程门槛

### bootstrap 门槛

- 目标是把仓库基础设施补齐
- 输出的是统一规则、上下文、profile、memory 骨架
- 没有这一步，后续两步没有稳定底座

### requirement 门槛

- 目标是把正式 PRD 沉淀成需求包
- 输出的是需求池、任务看板、设计目录、PRD 追溯、task memory
- 必须停在人工审核门

### execution 门槛

- 目标是把审核后的任务推进到实现收口
- 输出的是代码、验证、证据、状态、提交、闸门、归档
- 必须有显式开工指令

## 三段的输入输出

| 阶段 | 输入 | 输出 | 停止点 |
| --- | --- | --- | --- |
| bootstrap | 仓库根目录、profile 线索、宿主环境 | `AGENTS.md`、`PROJECT_CONTEXT.md`、`.ai/`、`doc/requirements/` | 初始化完成 |
| requirement | PRD、主题、仓库事实、profile | 需求池、任务看板、dated 需求包、task memory、handoff 结论 | 人工审核门 |
| execution | 审核通过、显式开工、任务看板、memory、profile | 实现、验证、证据、任务状态、commit、push、release gate | done / blocked |

## 关键目录

- `AGENTS.md`
- `PROJECT_CONTEXT.md`
- `doc/requirements/`
- `.ai/governance/`
- `.ai/memory/`
- `.ai/runtime/profile/`

## 关键文件

- `.ai/runtime/profile/project-profile.yml`
- `.ai/memory/tasks/index.md`
- `doc/requirements/需求池.md`
- `doc/requirements/任务看板.md`

## 统一入口

这三套 skill 在命令层不是彼此孤立的，统一入口由 `workflow-bootstrap` 目录下的 CLI 总控脚本串起来。

- `init / bootstrap`
- `health / doctor`
- `requirement / req`
- `execution / exec`
- `archive / arc`

## 这条链路的价值

- 让 AI 协作有统一事实源
- 让需求和实现有清晰边界
- 让验证、证据、记忆形成闭环
- 让项目在多人和多轮 AI 接力下仍然可控
