# 使用方法与误区

## 什么时候用哪个 skill

### 用 `workflow-bootstrap`

适合：

- 新仓库首次接入 workflow
- 老仓库补齐 `.ai/` 和 profile
- 想先把治理底座建起来

### 用 `workflow-requirement`

适合：

- 有正式 PRD，需要入需求池
- 需要拆任务看板
- 需要生成需求包和交接材料

### 用 `workflow-execution`

适合：

- requirement 已经审核通过
- 用户明确说“开干”
- 需要推进实现、验证、提交和闸门

## 推荐理解方式

不要把这三个 skill 当成“命令集合”，而要把它们看成三道门。

1. `bootstrap` 是仓库底座门
2. `requirement` 是需求治理门
3. `execution` 是执行收口门

## 操作节奏

在实际使用里，最稳妥的节奏是：

1. 先跑 `bootstrap`，把项目事实和治理底座补齐
2. 再跑 `requirement`，把 PRD 变成需求包和看板
3. 最后在审核通过后跑 `execution`，推进实现和收口

这不是练习题顺序，而是实际工作顺序。

## 典型使用方式

正常顺序是：

1. 先 bootstrap
2. 再 requirement
3. 最后 execution

不要反过来用。

## 常见场景和对应 skill

| 场景 | 适用 skill | 期望结果 |
| --- | --- | --- |
| 新仓库第一次接入 workflow | `workflow-bootstrap` | 建立治理底座和 profile |
| 老仓库补齐 memory / governance | `workflow-bootstrap` | 修复仓库事实和目录结构 |
| 正式 PRD 需要治理 | `workflow-requirement` | 生成需求包并停在审核门 |
| 审核后要开发收口 | `workflow-execution` | 完成实现、验证、留痕和提交 |
| 想看仓库 workflow 是否健康 | `workflow-bootstrap` 的 health | 找出缺失文件、legacy 路径和 profile 问题 |

## 最容易误用的地方

### 1. 没有 bootstrap 就直接 requirement

问题是缺少稳定的仓库事实、记忆层和 profile，后面治理会漂。

### 2. requirement 还没审核就进入 execution

问题是绕过了人工审核门，需求治理还没收口就开始实现。

### 3. execution 没有显式开工就执行

问题是会破坏阶段边界，容易把未确认任务直接推进。

### 4. 只改代码，不回写证据和 memory

问题是闭环不完整，后续接力和复盘都会失真。

### 5. 误把 task memory 当临时笔记

问题是 memory 是任务上下文的一部分，不是随手记一下就完了。

### 6. 把 release gate 当可有可无

问题是验证、证据和发布前检查会被弱化，任务只是“改完”，不是“收口”。

## 观察重点

分享时可以重点观察这几个信号：

- 是否明确区分了 PRD、需求包、任务看板、执行任务
- 是否理解人工审核门的作用
- 是否知道 release gate 为什么要存在
- 是否理解 evidence 和 memory 的差别
- 是否知道为什么 execution 默认动作更重

## 一句话总结

这三套 skill 的价值，不在于“自动干活”，而在于把 AI 协作开发变成一条有边界、有证据、有收口的流程。
