# 操作步骤说明

## 说明

这份文档不是练习题，而是给分享 PPT 提供“如何使用这三个 skill”的操作步骤。

## 1. 使用 workflow-bootstrap

### 适用场景

- 新仓库首次接入 workflow
- 老仓库补齐 `.ai/`、profile 和治理目录
- 需要自动识别旧底座、输出老项目画像和迁移建议

### 操作步骤

1. 进入仓库根目录，直接运行 `wf-init` 或 bootstrap 总入口，不需要先人工说明“这是新项目还是老项目”
2. 运行 bootstrap 总入口
3. 让脚本自动识别语言、构建工具、测试命令、PRD 目录、源码目录、旧底座状态和业务画像
4. 检查生成结果是否包含：
   - `AGENTS.md`
   - `docs/workflow/PROJECT_CONTEXT.md`
   - `docs/workflow/开发协作约定.md`
   - `.ai/governance/`
   - `.ai/memory/`
   - `.ai/runtime/profile/project-profile.yml`
   - `.ai/runtime/project-state.json`
   - `docs/workflow/legacy-analysis.md`
   - `.ai/runtime/cache/legacy-scan.json`
5. 运行 health check，确认缺失项和 legacy 路径是否已处理

### 操作结果

- 仓库具备统一协作规则
- 项目事实和 profile 落地
- 老项目业务域、关键接口链路和迁移建议被自动扫描出来
- 后续 requirement 和 execution 有稳定底座

## 2. 使用 workflow-requirement

### 适用场景

- 有正式 PRD，需要入需求池
- 需要拆任务看板和交接材料

### 操作步骤

1. 确认 PRD 已冻结，主题已明确
2. 运行 requirement 总入口，传入 `theme` 和必要的 `summary`
3. 让脚本找到合适的 PRD 路径并生成 `REQ-ID`、`TASK-ID`
4. 生成 dated 需求包目录
5. 填充需求池、任务看板、设计文档、PRD 追溯和测试材料
6. 自动读取 `.ai/runtime/cache/legacy-scan.json`，命中老项目业务域、接口链路和历史文档上下文
7. 初始化 task memory，并同步任务索引
8. 回写 `.ai/runtime/project-state.json`，把当前 requirement 状态和命中的 legacy context 沉淀进去
9. 执行交接检查，确认文档是否齐全
10. 停在人工审核门，等待显式开工前再进入 execution

### 操作结果

- 需求被正式入池
- 任务被拆解到看板
- 交接材料和 task memory 完成初始化
- 需求包自动带上老项目画像命中的上下文
- `project-state.json` 与驾驶舱看到的状态保持一致
- 流程停在审核门

## 3. 使用 workflow-execution

### 适用场景

- requirement 已通过人工审核
- 用户明确说“开干”

### 操作步骤

1. 确认当前任务和需求已通过审核
2. 运行 execution 总入口时加上 `--confirm-start`
3. 选择任务，优先 `doing`，否则最早 `todo`
4. 读取 memory、knowledge 和 legacy context，确认历史上下文
5. 跑构建和测试
6. 回写证据、verify、issue、decision、knowledge
7. 回写 `.ai/runtime/project-state.json`，同步当前执行态、任务状态和 legacy context 摘要
8. 必要时更新任务状态为 `blocked`
9. 成功时提交、推送并跑 release gate
10. 任务完成后根据需要归档 task memory

### 操作结果

- 实现完成
- 验证和证据同步落地
- 任务状态更新
- 驾驶舱读取的统一状态同步更新
- 提交和发布闸门结果明确

## 4. 使用顺序

正常顺序固定为：

1. `bootstrap`
2. `requirement`
3. `execution`

不要跳过 requirement 的审核门，也不要在没有显式开工时进入 execution。
